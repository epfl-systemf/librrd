the artifact
* explain old Racket version and new Scala.js version
  + global wrapping implemented only in Racket
    but nothing of the design of the Scala.js version prevents it
  + how to build
  + how to use
* reproduce all autogenerated figures in paper
* reproduce performance evaluation
  + extended: all SQLite diagrams?
    this would also help the next point
* justify SQLite counting in spreadsheet


LibRRD
======

Build: sbt deploy

CLI prereqs: node v20, npm
Set up CLI: npm install
Run CLI: node cli/cli.js [args…]

Run GUI: launch webserver in gui/ and open in browser
